source 'https://rubygems.org'

gem 'rake'
gem 'puppet'

# rspec-puppet > 1.0.0 is required so that the exception is reported through
# prior to that the catlog compilation error is masked with an weird error
# message about include
gem 'rspec-puppet',  
  :git => 'https://github.com/rodjek/rspec-puppet.git',
  :ref => '65e422a4a4129975458dcd1e304685ebcba21fd5'
